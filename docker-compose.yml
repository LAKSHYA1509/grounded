# For running the same image locally that gets deployed. Storage is remote
# (Qdrant Cloud), so there is no volume to mount - the container is
# genuinely stateless, which is what makes it safe to redeploy at will.
services:
  api:
    build: .
    ports:
      - "8000:7860"
    env_file:
      - .env
