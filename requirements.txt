# Pinned deliberately. Unpinned AI dependencies are the fastest way to have a
# repo that worked last week and doesn't today — these libraries move fast.
# If something breaks, you want to change one number, not debug a mystery.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1

langgraph==0.2.62
langchain==0.3.14
langchain-core==0.3.29
langchain-openai==0.2.14
langchain-chroma==0.1.4
langchain-text-splitters==0.3.5

pytest==8.3.4
httpx==0.28.1
